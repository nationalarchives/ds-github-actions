version: "3.8"
services:
  redis:
    image: redis/redis-stack-server:latest
    ports:
      - 6379:6379
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
    volumes:
      - redis_data:/data

volumes:
  redis_data:
